<!DOCTYPE html>
<html>
  <head>
    <title>LastCall</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://fonts.googleapis.com/css?family=Arvo&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= favicon_link_tag asset_path('favicon2.png'), :rel => 'icon', :type =>  'image/png' %>
    <% if logged_in? %>
      <script type="text/javascript">
        window.currentUser = <%= render(
          "api/users/user.json.jbuilder",
          user: current_user
        ).html_safe %>
      </script>
    <% end %>

    <script type="text/javascript">
         window.mastheadUrl = "<%= image_url('masthead-img-main.png') %>";
         window.lastcalllogo = "<%= image_url('lastcall-logo.png') %>";
         window.searchIcon = "<%= image_url('Search-icon.png') %>";
         window.beerIcon = "<%= image_url('beericon.png') %>";
         window.reviewIcon = "<%= image_url('review.png') %>";
         window.socializeIcon = "<%= image_url('socialize.png') %>";
         
         
    </script>

  </head>

  <body>
    <%= yield %>
  </body>
</html>
